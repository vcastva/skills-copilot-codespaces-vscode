// Create web server
// Create a file comments.js
// Create a web server that listens on port 3000
// When you visit http://localhost:3000/comments, it should return a list of comments
// The comments should be stored in a file called comments.json
// The comments should be read from the file and returned to the user
// If the file doesn't exist, it should return an empty array
// If the file is empty, it should return an empty array
// If the file contains invalid JSON, it should return an error